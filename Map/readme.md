The corresponding identifier can be found by sorting the data in ascending order of serial number.
In the association table, the first row contains the drug's CID in PubChem, and the first column contains the identifier in circBASE.
